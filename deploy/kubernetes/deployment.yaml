apiVersion: apps/v1
kind: Deployment
metadata:
  name: maintenant
  namespace: maintenant
  labels:
    app.kubernetes.io/name: maintenant
    app.kubernetes.io/component: monitoring
spec:
  replicas: 1
  strategy:
    type: Recreate # SQLite single-writer
  selector:
    matchLabels:
      app.kubernetes.io/name: maintenant
  template:
    metadata:
      labels:
        app.kubernetes.io/name: maintenant
        app.kubernetes.io/component: monitoring
    spec:
      serviceAccountName: maintenant
      securityContext:
        runAsNonRoot: true
        fsGroup: 65534
      containers:
        - name: maintenant
          image: ghcr.io/kolapsis/maintenant:v0.1.0
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: MAINTENANT_ADDR
              value: "0.0.0.0:8080"
            - name: MAINTENANT_RUNTIME
              value: "kubernetes"
            - name: MAINTENANT_DB
              value: "/data/maintenant.db"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: maintenant-data
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maintenant-data
  namespace: maintenant
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: maintenant
  namespace: maintenant
  labels:
    app.kubernetes.io/name: maintenant
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: maintenant
